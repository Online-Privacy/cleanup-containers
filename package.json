{
    "name": "cleanup-containers",
    "version": "1.0.0",
    "description": "Firefox extension to remove containers by regex with optional active-tab skipping",
    "author": "tekicat",
    "license": "MIT",
    "scripts": {
        "test": "jest",
        "web-ext:lint": "web-ext lint --source-dir ./src",
        "web-ext:build": "web-ext build --source-dir ./src --artifacts-dir=./out"
    },
    "jest": {
        "setupFiles": [
            "./tests/jest.setup.js"
        ]
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "sinon": "^20.0.0",
        "sinon-chrome": "^3.0.1",
        "web-ext": "^8.6.0"
    },
    "keywords": [
        "firefox-extension",
        "containers",
        "contextual-identities",
        "regex",
        "cleanup"
    ]
}
